---
term: loss
aliases: [losses, loss function, loss functions]
firstAppearance: optimization
---

**Loss** is another name for [error](/glossary#error).

The two mean exactly the same thing — a number that says how far off a model's output is from where we want it to be. ML textbooks tend to say "loss"; this tutorial says "error" because it's more intuitive.
