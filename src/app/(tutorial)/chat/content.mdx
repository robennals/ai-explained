The previous chapter ended with a machine that reads a passage of text and says what is likely to come next. That machine is not ChatGPT or Claude. This chapter is about the training that turns a next word predictor into something useful.

## What a Raw Model Does

A transformer (<Ch slug="transformers" />) trained to predict the next word (<Ch slug="next-word-prediction" />) is called a *base model*. This model has read an enormous amount of text and has formed a deep enough understanding of how the world works that it can predict the next work in a piece of text about pretty much anything.

But predicting the next word is not what you usually want a helpful assistant to do. The playground shows a few examples.

<RawCompletionWidget>
Switch between **base model** and **chat model** on each prompt. The base model's completion is not a mistake. Ask yourself where on the internet that text would have come next.
</RawCompletionWidget>

The base model is not confused and it is not stupid. It is doing exactly what we trained it to do. What we trained it to do was predict the next token, not to be a useful assistant.

## Post-Training

Closing that gap is a whole second stage of training. The two stages have names:

- **Pre-training** is next-word prediction over a very large body of text. It is where the model picks up its understanding of how the world works. Nearly all of the compute goes here, often more than 99% of it.
- **Post-training** is everything after that. It teaches the model how to apply that understanding to the thing you actually want the model to do. It is a thin layer, and it accounts for all of the difference between the two completions in the playground above.

There are several ways to do the second part, and a modern chat model has usually been through most of them. Most work the same way underneath: produce several candidate responses, score them somehow, and adjust the model's weights so that higher-scoring responses become more likely.

<TrainingSignalWidget>
Step through the five tabs. Each is a different way of telling the model which answer you wanted, and the last two rest on the same idea: checking an answer is far easier than producing one.
</TrainingSignalWidget>

Post-training does not teach the model new facts or new ways of understanding the world. It teaches it how to apply the understanding it already has to do something useful.

## Making It a Conversation

Underneath every chat interface there is a single stream of tokens. The model does not see turns. It sees one document with markers in it saying who is speaking.

<TranscriptViewsWidget>
The same conversation both ways. The markers are real tokens, and the system prompt is text like everything else. Note the dashed message in the second view: it is in the transcript, but the human never sees it.
</TranscriptViewsWidget>

The format is called a *chat template*. Every turn is opened and closed, and the markers are real tokens the tokeniser knows about. What differs is who writes them. The program running the model writes the system and human turns, and the marker that opens the model's turn. The model writes its reply, and then the marker that closes it, which is how the program knows the reply has finished.

The exact markers vary by model, though the shape does not. Llama 3 opens a turn with `<|start_header_id|>user<|end_header_id|>` and closes it with `<|eot_id|>`. The format OpenAI called ChatML, which many open models adopted, looks like this:

```
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
What's the capital of Australia?<|im_end|>
<|im_start|>assistant
Canberra.<|im_end|>
```

Post-training is what taught the model to continue after the assistant marker, to answer rather than carry on inventing the human's messages, and to stop where it should.

The system prompt is nothing more special than text at the top of that document. That is why it works, and also why it is not a hard guarantee. Everything the model knows about the conversation is in the transcript in front of it.

From here on, this tutorial draws conversations as messages rather than as raw tokens, because they are easier to follow. Every one of them is really one stream of text like the one above, and a message drawn with a dashed outline is one the human is never shown.

## Putting It Together

A chat model is a next-token predictor with two things wrapped around it. Post-training aims it at answering rather than at continuing a document. A chat template arranges the exchange as one document for it to continue.

The transformer is doing the same thing it was doing at the end of <Ch slug="transformers" />, predicting one token at a time. What changed is what it gets asked to predict.

The next two chapters add the rest of what a chat product does. <Ch slug="reasoning" /> gives the model room to think before it answers. <Ch slug="agents" /> lets it act on the world rather than only talk about it.
